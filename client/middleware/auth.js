export default function({ route, redirect }) {
    // Set route paths you want to protect
    // const protectedRoutes = ['/publication/create', '/mi-cuenta','/mis-publicaciones'];
    // if (protectedRoutes.includes(route.path)) {
    //     const isLogged = !!localStorage.getItem("userInfoToken");
    //     if (!isLogged) {
    //         return redirect('/login');
    //     }
    // }
}